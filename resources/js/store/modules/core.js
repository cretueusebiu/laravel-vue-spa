// state
export const state = {
  baseUrl: '/api/v1/'
}

// getters
export const getters = {
  baseUrl: state => state.baseUrl
}

// mutations
export const mutations = {}

// actions
export const actions = {}
